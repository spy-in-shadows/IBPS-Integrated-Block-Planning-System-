# Public URL of the deployed FastAPI service, including its /api prefix.
# Do not put secrets in VITE_ variables: they are included in the browser bundle.
VITE_API_BASE_URL=https://your-api.onrender.com/api
